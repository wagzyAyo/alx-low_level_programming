#include "main.h"

/**
 * _val - computes the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of int
 */
int _val(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
